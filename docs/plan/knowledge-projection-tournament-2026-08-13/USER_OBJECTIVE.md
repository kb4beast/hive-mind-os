# User Objective

## Provenance

- Source kind: direct user statements in the current Codex task.
- Captured by: `codex:orchestrator`.
- Capture date: 2026-08-13.
- Repository baseline requested by the user:
  `a93df2632f259f4b63f7a4f27eb0b163b5a47204` on `origin/main`.
- Availability: the statements below were present in the task; no absent source
  content has been reconstructed.

## Exact source excerpts

> we need to put into obsidian all the champion challenger ideas so its not repeated. this app can be used for any other repo as a tool, but if there are lessons to be learned then it should not publish secret or IP/Propriatary info to this repo. Redraw again

> what else should write to obsidian, for learning, and other reasons like tracking end to end what hive mind even considered and its reasons for going backwards in workflow

> ideas can go through a loop multiple times

> remember this as the latest idea of where to take this repo.

> first pull latest main branch. then from what i told you to remember about the the latest idea of where to take this repo, use that as the subject for the DAG tournament. create a dag and plan

## Clerk's atomic extraction

The extracted subject is a repository-neutral Hive Mind OS knowledge system with:

1. a protected, append-only, per-repository canonical knowledge plane;
2. a generated Obsidian-compatible view that is not authoritative;
3. stable idea identities with immutable repeated passes and explicit reasons for
   every remand, reversal, appeal, or re-entry;
4. end-to-end links among sources, claims, roles, courts, work, candidates,
   verification, delivery, outcomes, lessons, champions, and challengers;
5. a shared cross-repository learning registry containing only independently
   approved, allowlisted abstractions; and
6. a fail-closed boundary that prevents secrets, private/customer data, source or
   patch bodies, prompts, proprietary material, incompatible-license content, and
   identifying repository details from crossing into shared or public state.

This extraction is a proposed interpretation for adjudication. The quoted statements
remain the source of truth.
