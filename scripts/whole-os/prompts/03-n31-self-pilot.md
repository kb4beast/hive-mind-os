Start or resume N31, the real 72-hour self-repository pilot, using the durable state
provided by the launcher. Read `AGENTS.md`, the implementation prompt, N31 contract,
pilot runtime/orchestration code, and current receipts. Work directly with one agent
and make useful repository changes rather than producing plans about changes.

Use the admitted N30 candidate and trusted Whole-OS host. Execute three distinct
nontrivial change families against this repository within the configured concurrency,
daily resource, delivery, and authority limits. Create bounded branches/PRs with the
existing permitted GitHub credentials, preserve restart and rollback evidence, and
measure duplicate effects, avoidable owner questions, failures, interventions, and
outcomes. Do not merge a protected branch or compress the required elapsed window.

Commit/push durable pilot artifacts and repairs. Return `in_progress` with an
efficient retry interval while the real window is still open. Return complete only
after at least 72 actual hours and all N31 acceptance evidence exists. Treat a missing
host, N30 candidate, delivery grant, or supervisor rollback pointer as a typed blocker.
