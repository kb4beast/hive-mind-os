# Cross-language monorepo fixture

The repository contains Python, Node, and C# components. Acceptance must preserve component
boundaries and report each language rather than treating the root README as proof of coverage.
