# Separate controller campaign after promotion-binding delivery

Status: deferred follow-up; no controller implementation or execution authority
is included in the promotion-binding repair batch.

After this PR is independently reviewed and its verification results are retained,
open a separate campaign to design durable continuation of already authorized
routine work. Start from the delivered promotion-binding commit and retained PR
review, verification receipts, dissent and unresolved evidence obligations.
Do not revive obsolete dispatcher releases or failed tournament execution state.

The separate proposal must specify a versioned checkpoint with the live scope,
authority boundaries, pending evidence, next reversible operation, attempt budget
and typed blocker. Re-observe repository/worktree identity and current control
state before resuming. An observation-recovery operation must remain distinct
from a new action: use committed admission identifiers and the receipt-recovery
API rather than replaying evaluation records or principal nonces.

Acceptance for that campaign must demonstrate restart after each checkpoint,
no duplicate side effects, preserved rejection/dissent evidence, exhaustion and
denial without repeated retry, and a stop on changed scope or ambiguous state.
It must leave Git delivery ownership and protected merge/deployment/credential
boundaries explicit. Independent review, rollback design and applicable role
evidence belong to that campaign. No new controller, scheduling service, authority
bootstrap, live promotion or protected merge is authorized by this document.
