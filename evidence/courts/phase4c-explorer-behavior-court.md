# Phase 4C Explorer development evaluation court

## Case

`P4C-001` asks whether Hive Mind OS may add an inert, development-visible
evaluation substrate without misrepresenting Explorer v2 as executed or evaluated.

Explorer/Clerk/Advocate `/root/phase4b_optimizer` and Architect
`/root/phase4b_curator` independently recommend `adapt` of five strict contracts and
a pure scorer. Both reject using Phase 4A's injected shadow engine as proof of
Explorer v2 behavior and require candidate observations to fail closed while its
runtime binding is absent.

## Builder claim

Builder `/root` adds only package-private Python contracts/scoring and focused
tests. The fixed suite and subject digests, eleven-family order, one-million-ppm
weights, hard safety floors, candidate `forced-not-run` state, comparison `not-run`,
defensive copying, and no-authority/no-promotion boundary are executable.

This claim is structural only. It does not claim actual discovery, duplicate
avoidance, provenance behavior, injection resistance, stopping, loop control,
token accuracy, memory-contamination resistance, usefulness, value, learning,
promotion, activation, or superiority.

Independent Cross-Examiner, Curator/Expert, Integrator, Steward, Optimizer, and
Judge receipts remain pending.

## Cross-examination

Independent Cross-Examiner `/root/phase4c_cross` recommended `adapt` only after
mandatory repair of checkpoint `3889c83`. It demonstrated two correctly resealed
forged measurements that passed standalone validation, missing observation-to-
metric provenance, an unintended packaged-resource read in the scorer, and hostile
string-subclass method execution.

Builder repair rederives measurement semantics from fixed identities and weights,
binds each measured metric to the exact observation and execution pins, removes
successor compilation/resource reads from scoring, and rejects non-built-in scalar
inputs. The original defer remains preserved; the same Cross-Examiner must
independently reproduce closure.

The first recheck closed three remands but kept exact provenance open because
detached metrics could reseal copied fields. The second repair embeds each validated
source observation in the measurement and rederives all one-to-one links and fixed
case pins. This additional remand and repair remain append-only pending final Cross-
Examiner closure.

At implementation `130d9d4`, the same Cross-Examiner rejected duplicate or altered
observation identities/digests/cases, fixed and execution pin changes, missing
observations, detached metrics, and altered one-to-one provenance. It closed every
remand and changed recommendation to `adapt`, retaining all development-visible,
non-holdout, no-comparison, no-promotion, and no-activation limits.

A separately identified Curator then reproduced a new resealed-receipt attack at
`130d9d4`: an embedded authority observation could report a failed assertion and
`unauthorized-action` while the linked metric retained a perfect score and no
violations. Standalone measurement validation checked identity and provenance but
did not rederive metric results. The Builder remanded the proposal, added exact
result rederivation and an exploit regression, and produced repaired implementation
`175e2d2`. Independent Curator closure remains pending.

The separately identified Integrator recommended `adapt` at `175e2d2` with no
integration remand. It verified the exact Phase 4B stack base, frozen supported
surfaces and generic evaluators, five explicit v1 contracts, deletion-only rollback,
and focused Phase 4C and compatibility checks. Publication must be an inactive
draft stacked on `codex/phase4-explorer-v2-candidate`; all behavioral, comparison,
value, learning, promotion, activation, champion, and superiority claims remain
deferred.

The separately identified Steward recommended `adapt` at `175e2d2`, conditional
on final inventory resealing. It reproduced deterministic incomplete/not-run output
with no filesystem, process, network, or OS audit events and found deletion-only
rollback credible. It preserved nonblocking debt for duplicated scorer/validator
derivation and a broader future no-I/O guard.

The separately identified Optimizer recommended narrow `adapt` at `175e2d2`.
The integer vector measures only caller-supplied, development-visible contract
outcomes. It cannot support behavior, learning, value, generalization, candidate
quality, or superiority claims. Any later evaluation requires governed holdouts,
real runtime receipts, semantic artifact verification, repeated equal-budget trials,
multiple comparators, independent evaluation, and customer-outcome calibration.

The separately identified Curator closed its mandatory semantic remand at exact
`175e2d2`. It repeated a stronger fully resealed authority attack, updated the
linked observation digest, resealed the metric and measurement, and observed
rejection specifically because the metric result did not match the observation.
Relevant Phase 4A-C, static/type, no-I/O, and clean-wheel checks passed; the wheel
retained 133 JSON resources and both private modules. Its functional recommendation
is `adapt`; final inventory resealing is the only remaining mechanical obligation.
