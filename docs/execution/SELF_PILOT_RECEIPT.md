# Self-pilot receipt

Status: `BLOCKED_AUTHORITY`

The `PilotReport` contract requires a real 72-hour window, at least three accepted
nontrivial attempts, restart recovery, zero duplicate effects, zero avoidable owner
questions, delivery receipts, and rollback evidence before it can return `adopt`.

No self-repository production lease, supervisor release-pointer grant, actual PR
receipts, or elapsed observation window has been supplied. No pilot success is
claimed by the implementation.
