# Short Token-Aware Next Sessions

Use these after the bootstrap PR is merged. Run the dispatcher first; exact current repository truth overrides expectations.

## Dispatcher — ChatGPT Classic

```text
Repo kb4beast/hive-mind-os. Act only as Autopilot dispatcher. Read AGENTS.md, .autopilot/README.md, .autopilot/workflow-policy.json, .autopilot/plan.json, and docs/execution/GENERICPROMPT_CONTEXT_INDEX.md. Reconcile exact current main, PRs, branches, CI, claims, and validated receipts; run doctor/status/ready. Do not implement or claim a node. ChatGPT Classic owns all work; Codex is only a smallest-possible last-resort subtask after Classic tools and role consultation are exhausted. If human action is truly required, give novice-safe exact steps. Return ready nodes, minimum model effort, and copy-ready prompts. End with WHAT I DID, NEXT STEPS, BLOCKS.
```

## RECON-010 — ChatGPT Classic

```text
Repo kb4beast/hive-mind-os. Execute only RECON-010 after BOOT-000 is merged. Read AGENTS.md, .autopilot/README.md, .autopilot/workflow-policy.json, and the RECON-010 contract in .autopilot/plan.json. Reconcile exact current main plus PR/branch/CI/receipt/claim state and plan-impacting work; preserve uncertainty and change no product runtime. Use Classic/tools and role consultation first. If one local-only capability remains, emit only a short CODEX SUBTASK for that action and resume here after evidence returns. Open a draft node PR with validated receipt; do not merge or start ARCH-100. If human action is unavoidable, give exact novice-safe steps. End with WHAT I DID, NEXT STEPS, BLOCKS.
```

## BASE-020 — ChatGPT Classic

```text
Repo kb4beast/hive-mind-os. Execute only BASE-020 after BOOT-000 is merged. Read AGENTS.md, .autopilot/README.md, .autopilot/workflow-policy.json, and the BASE-020 contract in .autopilot/plan.json. Capture the exact clean test/runtime/provider baseline and classify all eight roles by real planner/provider/tool/effect/receipt/consultation/evaluation/acceptance wiring. Preserve adverse evidence and do not repair product behavior except characterization defects. Use Classic/tools first; Codex is only for the smallest local-only test/build/shell action Classic cannot perform. Open a draft node PR with validated receipt; do not merge or start ARCH-100. If human action is unavoidable, give exact novice-safe steps. End with WHAT I DID, NEXT STEPS, BLOCKS.
```
