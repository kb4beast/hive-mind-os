Execute N30, the measured Whole-OS tournament, end to end. Read `AGENTS.md`, the
complete implementation prompt, the N02 and N30 node contracts, current benchmark
code, and retained benchmark evidence. Work directly with one agent; do not stop
at planning or manufacture review ceremony.

Materialize only exact admitted comparator recipes. Record source version, digest,
license/reuse status, evaluator custody, lane admission, lease, task/family/block
identity, model/configuration, costs when available, and every failure. Use the
existing execution broker and installed Codex runtime. Run the real lanes, persist
durable intent before effects, reconcile uncertain outcomes before retrying, and
produce the measured court result. Never relabel unit fixtures as real comparator
receipts or claim superiority without the required independent receipts.

Implement and test any bounded local repair needed to run the admitted protocol,
commit it on the current branch, push, and update the same scoped PR. Report complete
only when an N30 candidate is actually admitted or the evidence-backed court
concludes with a terminal non-promotion disposition. A missing external source,
rights grant, or evaluator lease is a typed blocker, not a reason to guess.
