# Phase 12 retained failed benchmark materialization

This is the failed in-repository materialization of benchmark run
`p13-62b18c75e8cc99fc`. It is kept under a deliberately short path because the
point-in-time oracle copies the repository into a temporary Windows path; the original
deep evidence path exceeded that platform's path limit. The bytes and failure outcome are
unchanged. The fresh-root successful rerun is retained separately at
`evidence/local_assurance/phase12-9efe64b/benchmark-success/`.
