# Original owner request

Source: current conversation, owner-supplied requirement, received 2026-09-13 America/Chicago. Preservation: exact substantive message below, including original spelling and escaped-space text. Authority scope for this task: documentation handoff only.

> run the tournament on hivemindos in total. i want for this repo to be an independent AI operating system that can run all on its own, finding and implementing its own things and opening its own PRs.  i dont want it to have to many constraints and should be able to be extremly efficient and fast. this OS must not only be able to run on itself and for itself, but should also be able to run against other repos as well.  hive mind must be able to implement code on its own that is prod ready.&#x20;
>
> for example one goal i have is to have it run and build fully production roblox games, we will back train it by giving examples of live roblox repos only the first and last commit along with its read me, then the code base should get smarter and smarter. sometimes hivemindos needs to get smarter, and sometimes the app that hivemind is working on must be able to get smarter on its own without leaking info into hivemindos like any kind of secrets. but anybody using hive mind for any other app other than hive mind os then hive mind must check in a lessons learned file as draft mode (it wont check in code, just what it can learn). this is just one example, but you must understand  the intent.&#x20;
>
> when you build this ultimate tournament , make sure to not give it such tight implementations because in the past there was a lot of token wasting when checking and rechecking every single little thing. I want this new enhancement to be more lose during the implementation stages/plans of executing the dag.&#x20;
>
> do not implement any code just build an extremly detailed runbook handoff so  much lower llm model can implement. for each node give details so there is no ambiguity and every implementation step is trivial .

The original is retained separately from the atomic requirement interpretation in requirements.json. The handoff does not amend the founding contract or activate existing automation.
