def checkout_total(subtotal_cents: int, *, nonprofit: bool) -> int:
    """Return the checkout total in cents."""

    # TODO: nonprofit organizations receive the documented 20 percent discount.
    return subtotal_cents
