# Combined prerequisite qualification — exact `ca437095` tree

## Status and scope

This is an additive integration evidence packet, not a self-issued promotion.
Independent Advocate, Cross-Examiner, Expert, Curator, and Judge dispositions are
required before the combined prerequisite can be called adopted.

It composes three independently developed prerequisite changes on the exact tree at:

- commit `ca43709591313c1c166a2e655b8982ccff16daf3`;
- tree `22639258c7a524ffda25272ccf34fede176b2663`; and
- branch `release/hive-mind-autopilot`.

The three changes are:

1. new-subject safety, integrated as `292e12386e91d98c4494d3222e05784125baaed7`;
2. DAG Authoring Standard V2, integrated as
   `eaa7f3f3c897dda1a54c61f5983045e0bd427633`; and
3. bounded doctor execution, integrated as
   `ca43709591313c1c166a2e655b8982ccff16daf3` from independently qualified source
   commit `762954a5201eb8f585d9782665064e2224b2a738`.

The report binds only the exact local prerequisite payload and the named local test
observations. It does not authorize a push, pull request, protected merge, deployment,
credential use, spending, policy mutation, or production-readiness claim.

## Exact combined Payload A

The ordered line-manifest format is one lowercase SHA-256 digest, two ASCII spaces,
the UTF-8 forward-slash path, and LF for each record. Its length is 1,292 bytes and
its SHA-256 is
`5bb32f05b65c975fe90cb4ad21644bbf84363aed362757e4487afd590d6a019b`.
The concatenated raw file bytes are 611,066 bytes with SHA-256
`e0000dbdc047dc8053ea0e6782feee610dabfac8e9d65f4db76eddbb6801682a`.

| Path | Bytes | SHA-256 | Git blob |
| --- | ---: | --- | --- |
| `src/hive_mind_os/autopilot_workflow.py` | 60,274 | `36a337ae968e0c0e40a1c94d78c5b85a4b8184af7d35efb8a5e0548c52d1b67f` | `92a07336fbc59cf9a097a6f5693d9f96b01f2ce1` |
| `tests/test_autopilot_workflow.py` | 32,182 | `dee392e1e38286b28f0c7f6df4107eb111f928d9bd0bb839dc39966dca6b3568` | `0d1a9a1a795b6222ffe00da396e9198fa10035f2` |
| `.autopilot/bin/dag_standard.py` | 104,317 | `105674faf15aaf7b9f4c9db7ad4003fda404438eed2bf8cc3a1782c1cf321e6a` | `f170ac4f388d265fcaafd32437e449945dcebee3` |
| `.autopilot/tests/test_dag_standard.py` | 88,437 | `63f5690480889aac0c344d84a78c54185e4463af8421ef2e3a6ea3637b400f07` | `f308c89f079482fb81dd31b54ac0a4467c27f1a9` |
| `docs/architecture/ADR-067-DAG-STANDARD-V2-TYPED-DURABILITY-AND-BOUND-CONSUMPTION.md` | 5,679 | `8d2becc7df831855d7d07fa13a33155f7d0f5faf2a7dc877b5d8d173c0d74be2` | `5ef20b86ff816c5c2ad866839cdc9aa111ec89b5` |
| `docs/execution/DAG_AUTHORING_STANDARD_V2.md` | 12,312 | `3b072fee295e75b8c28709d417f9036fa384e31dc53ca85526babd0881d0e90a` | `2bc9c0fa3baf6fb5cc720ffdbf7528e93f4e7374` |
| `.autopilot/README.md` | 20,212 | `afce0adae94847e5911a1e9390df2d2fab89ec662c3bf1e970f68987fdc7b417` | `6933885d91468e99704bd45203dd30da326de7d5` |
| `.autopilot/bin/autopilot.py` | 66,963 | `e93a8ff557500456eed27be88a5f1d3c5d8e8306c9379aa2d65503b3aa95b3e9` | `c7193136764e4a8e47f68240aa7a1ef661284b6c` |
| `.autopilot/bin/controller.py` | 170,351 | `08230e8638296d000cb0e25c53b253f2a03dfc94d2a7770ebb471a941329512b` | `06b595096e514f2a9abb4d15ef0d6a9621b36d73` |
| `.autopilot/tests/test_doctor_boundary.py` | 27,932 | `ca455bc24c8f0f6d2ef548a4c36fed596d603438c2f1e357f24c12bd61ea6694` | `18b9bdd258b1cc292931c6808f4605cb3dc1553a` |
| `docs/architecture/ADR-068-BOUNDED-DOCTOR-EXECUTION-AND-EXACT-CHECKOUT-BINDING.md` | 11,211 | `73eb89da303c1af3d6aad736a8e6c06f95d9a99b89603088b2b1a69cdd087674` | `1bd4967ed8217ed3e95e687ee321cd5e2b781ff4` |
| `docs/architecture/ADR_INDEX.md` | 11,196 | `a7ba9fc1409edfe537b269316fbd10f78792ab76ecc848802103442dd7fce360` | `3d957560aae6dd644d975ca8695df85f60482e79` |

This payload excludes this report and any later court receipt to avoid self-reference.
Those records are Envelope B: they may bind Payload A but cannot enlarge its test claim.

## Integration composition finding

The only cherry-pick conflict was `docs/architecture/ADR_INDEX.md`. The resolved file:

- retains ADR-066 and ADR-067;
- adds ADR-068;
- describes each prerequisite as a qualified local candidate whose protected promotion
  remains pending; and
- advances the next identifier to ADR-069.

That resolution is semantically consistent, but it is not byte-identical to either
source court's frozen shared index:

- current combined index: 11,196 bytes,
  `a7ba9fc1409edfe537b269316fbd10f78792ab76ecc848802103442dd7fce360`;
- doctor R3 index: 10,902 bytes,
  `bd2a5be0e4fd90b2fc98d154ca176496e21547ec98303e8c2af6052da7f226e4`;
  and
- DAG V2 court index:
  `543307390a998ce388eede628c35289d65989a05122eff574b1da1c66106621c`.

Therefore neither prior exact-byte court alone qualifies the combined tree. This
packet supplies a new exact combined identity and preserves that integration objection.

The doctor controller, doctor boundary tests, V2 compiler, V2 tests, V2 standard, and
both ADR bodies remain byte-identical to their independently qualified source payloads.

## Validation observations

### Focused suites

- DAG Standard V2: 106 tests passed.
- Doctor boundary: 25 tests run, 24 passed, one expected POSIX-only skip on Windows.
- Portable Autopilot workflow with exact checkout binding: 26 tests run, 25 passed,
  one expected Windows symlink-privilege skip.

The first combined workflow invocation was adverse: ambient Python imported
`C:\Repos\HiveMind\hive-mind-os\src\hive_mind_os\autopilot_workflow.py` and produced
five failures plus one error. A direct probe proved the foreign editable install in
`sys.path`. The exact rerun set
`PYTHONPATH=C:\Users\beesp\.codex\worktrees\8d97\hive-mind-os\src`; both
`hive_mind_os.__file__` and `autopilot_workflow.__file__` then resolved to the exact
`8d97` checkout, and the suite passed. The adverse run is retained as environment
contamination evidence; it was not relabeled as a source failure or discarded.

### Exact-tree repository CI

Command:

```powershell
$env:PYTHONPATH='C:\Users\beesp\.codex\worktrees\8d97\hive-mind-os\src'
Remove-Item Env:GIT_PAGER -ErrorAction SilentlyContinue
C:\Python314\python.exe -m unittest discover -s tests -v
```

Observed result on unchanged commit `ca43709591313c1c166a2e655b8982ccff16daf3`:

- exit code: 0;
- tests: 1,123;
- skipped: 7 platform-dependent cases;
- duration: 1,091.628 seconds; and
- terminal summary: `OK (skipped=7)`.

The unified execution stream was observed live but no independent raw transcript or
transcript digest was retained. This supports the narrow command/exit/count/duration
claim only. It does not support transcript-byte integrity, complete per-test output,
or independent replay.

### Exact-tree full doctor

The qualifying durable run is:

`C:\Users\beesp\AppData\Local\HiveMindOS\evidence\durable-doctor-capture\runs\combined-ca437-doctor-20260823T120447-argv`

Bindings and results:

- harness SHA-256:
  `bf873fb4ecba27fc5ff4caf7bef55f0a78c37a2c28e19354138c83d4f1504f5b`;
- receipt: 6,060 bytes,
  `6270dc7ca795bb15c5ff85997edacf6efa3fc88433ac917cd6d0b62985a99aa1`;
- stdout: 3,372 bytes,
  `db9204a29f6ef006cf4f6b3023e937dd3bfb6b2de6538d547ca3a06f411f4394`;
- stderr: zero bytes,
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- helper and child exit: 0;
- `qualifying_capture`: true;
- `state`: `READY`, `validation_scope`: `full`, `passed`: true;
- all six checks passed: configuration, repository, receipts,
  consultation-contracts, runtime-coordination, and controller-tests;
- controller tests ran in the isolated exact checkout in 311.496 seconds;
- ambient and isolated package origins both resolved to this worktree;
- natural accounting drain, no forced termination, zero live survivors, and
  `tree_empty: true`; and
- total helper duration: 316,885.9243 milliseconds.

One preceding durable attempt is deliberately retained as non-qualifying adverse
evidence. Native PowerShell argument marshalling collapsed the Python argv into one
string; child exit was 2. Its receipt is 6,057 bytes with SHA-256
`bf06e9b4b1957796be44af50e1d61b166964245cd3d2621e1fb0553d83f268ba`,
and its 257-byte stderr SHA-256 is
`242da62a75522bf944d04cb4799faa6aacd4a40dd8a5463875e6a6a59c279e72`.
An earlier Windows PowerShell 5.1 invocation stopped before run creation because that
runtime lacks `System.IO.Path.IsPathFullyQualified`; it is a launcher-version failure,
not a doctor observation.

## Retained dissent and nonclaims

1. `git diff eaa7f3f3c897dda1a54c61f5983045e0bd427633
   ca43709591313c1c166a2e655b8982ccff16daf3 --check` exits 2 because ADR-068
   line 146 contains one trailing space. Those bytes are part of qualified doctor R3;
   changing them here would silently create an unqualified substitute. This known
   formatting defect is retained and must be named in any integration verdict.
2. The Windows controller retains the already-adopted exceptional-path dissent: a
   `CloseHandle` failure is typed but cannot prove descendant cleanup after the raw
   handle is forgotten. No new handle regression was found in the integration.
3. POSIX process groups cannot contain a descendant that calls `setsid`; the full
   doctor makes no stronger claim.
4. The user request under `.hive-mind/autopilot-request.json` is untracked local state
   and is not part of Payload A. It must not be staged accidentally. Its historical
   observed head remains the request snapshot, not a claim that it equals the current
   integration head.
5. Existing controller trust is stale after controller-bundle changes. This local
   qualification does not create or imply fresh host execution trust.
6. The combined prerequisite does not make the later generated DAG or its mutable
   candidate controller eligible to qualify itself. A V3 run must freeze an externally
   reviewed host compiler/controller bundle before target mutation and use it for every
   scheduling and integration decision.
7. No full-autonomy, production-readiness, release-readiness, or superiority claim is
   made by this prerequisite packet.

## Rollback

Rollback is ancestry-preserving revert, in reverse integration order, of
`ca43709591313c1c166a2e655b8982ccff16daf3`,
`eaa7f3f3c897dda1a54c61f5983045e0bd427633`, and
`292e12386e91d98c4494d3222e05784125baaed7`. Preserve this report, all prior courts,
the adverse import observation, non-qualifying harness receipt, and qualifying doctor
receipt as append-only evidence. Never reset, squash, amend, or rewrite the source
commits to make their original payload manifests appear to cover the combined index.
